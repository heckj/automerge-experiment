import XCTest
@testable import automerge_experiment

final class automerge_experimentTests: XCTestCase {
    func testExample() throws {
        XCTAssertEqual(automerge_experiment().text, "Hello, World!")
    }
}
